apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compile project(path: ':router-annotation')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
